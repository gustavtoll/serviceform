{
  "name": "serviceform-prospecting-playbook",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "check": "npm run build",
    "deploy:preview": "npx --yes vercel",
    "deploy:production": "npx --yes vercel --prod"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
