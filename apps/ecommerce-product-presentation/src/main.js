const slides=[...document.querySelectorAll('.slide')];const counter=document.querySelector('#counter');const progress=document.querySelector('#progress');const previous=document.querySelector('#previous');const next=document.querySelector('#next');let current=0;
function show(index){current=Math.min(slides.length-1,Math.max(0,index));slides.forEach((slide,i)=>{slide.classList.toggle('active',i===current);slide.hidden=i!==current;slide.setAttribute('aria-hidden',String(i!==current));});counter.textContent=`${current+1} / ${slides.length}`;progress.style.width=`${((current+1)/slides.length)*100}%`;previous.disabled=current===0;next.disabled=current===slides.length-1;slides[current].focus({preventScroll:true});}
function isInteractiveControl(target){return Boolean(target?.closest?.('button,a,input,select,textarea,[role="button"]'))||['BUTTON','A','INPUT','SELECT','TEXTAREA'].includes(target?.tagName);}
previous.addEventListener('click',()=>show(current-1));next.addEventListener('click',()=>show(current+1));document.addEventListener('keydown',(event)=>{if(isInteractiveControl(event.target))return;const actions={ArrowRight:1,PageDown:1,Space:1,' ' :1,ArrowLeft:-1,PageUp:-1};if(event.key==='Home'){event.preventDefault();show(0);}else if(event.key==='End'){event.preventDefault();show(slides.length-1);}else if(actions[event.key]){event.preventDefault();show(current+actions[event.key]);}});show(0);
