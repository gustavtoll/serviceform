const status=document.querySelector('#filter-status');status.setAttribute('aria-live','polite');document.querySelectorAll('[data-filter]').forEach((button)=>button.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach((item)=>item.classList.remove('active'));button.classList.add('active');status.textContent=`Showing ${button.dataset.filter==='all'?'all':button.dataset.filter} records: 0.`;}));
document.querySelector('#copy').addEventListener('click',async()=>{const output=document.querySelector('#copy-status');try{await navigator.clipboard.writeText(document.querySelector('#template').value);output.textContent='Blank template copied.';}catch{output.textContent='Copy unavailable. Select the template text manually.';}});
