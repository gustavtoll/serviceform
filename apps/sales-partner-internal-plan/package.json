{
  "name": "serviceform-sales-partner-internal-plan",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "test": "node tests/static-validation.mjs",
    "check": "npm run test && npm run build"
  },
  "devDependencies": { "vite": "latest" }
}
