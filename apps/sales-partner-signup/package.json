{
  "name": "serviceform-sales-partner-signup",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "test": "node tests/pilot-api.test.mjs && node tests/static-validation.mjs",
    "check": "npm run test && npm run build"
  },
  "devDependencies": { "vite": "latest" }
}
