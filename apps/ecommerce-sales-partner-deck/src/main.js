const slides=[...document.querySelectorAll('.slide')],counter=document.querySelector('#counter'),progress=document.querySelector('#progress');let active=0;const show=i=>slides[Math.max(0,Math.min(slides.length-1,i))].scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){active=slides.indexOf(entry.target);counter.textContent=`${String(active+1).padStart(2,'0')} / ${slides.length}`;progress.style.width=`${(active+1)/slides.length*100}%`}}),{threshold:.55}).observe(slides[0]);const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){active=slides.indexOf(entry.target);counter.textContent=`${String(active+1).padStart(2,'0')} / ${slides.length}`;progress.style.width=`${(active+1)/slides.length*100}%`}}),{threshold:.55});slides.slice(1).forEach(s=>observer.observe(s));document.querySelector('#prev').addEventListener('click',()=>show(active-1));document.querySelector('#next').addEventListener('click',()=>show(active+1));addEventListener('keydown',e=>{if(['ArrowRight','PageDown',' '].includes(e.key)){e.preventDefault();show(active+1)}if(['ArrowLeft','PageUp'].includes(e.key)){e.preventDefault();show(active-1)}if(e.key==='Home')show(0);if(e.key==='End')show(slides.length-1)});
